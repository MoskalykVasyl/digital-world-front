export type AuthFormData = {
    email: string;
    password: string;
  }

  export type RegisterFormData = {
    email: string;
    password: string;
    fullName: string;
    avatartUrl?: string;
  }